'use babel';

import ZenkbarieswordcountView from '../lib/zenkbarieswordcount-view';

describe('ZenkbarieswordcountView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
